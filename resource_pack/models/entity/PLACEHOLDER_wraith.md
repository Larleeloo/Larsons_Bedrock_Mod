# BlockBench export: drop `wraith.geo.json` here

From BlockBench:
1. File → Export → **Bedrock Geometry** (*.geo.json)
2. Save as: `wraith.geo.json` in this directory

The model's identifier **must** be `geometry.wraith` (matches `wraith.entity.json`).
Delete this placeholder once the geo file is in place.
